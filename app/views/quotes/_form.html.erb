<%= form_with(model: quote, local: true) do |form| %>
  <% if quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
        <% quote.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!-- <div class="field">
    <%= form.label :premium %>
    <%= form.text_field :premium %>
  </div> -->
<br>
  <div class="field">
    <%= form.label "Phone Model" %>
    <%= form.collection_select :ex_model_id, ExModel.all, :id, :model %>
  </div>
  <br>

  <div class="actions">
    <%= form.submit "Submit", :class => 'submitButton' %>
  </div>

  <div class="refuseLink">
  <p onclick="redirectPay()" style="font-size : 11px">No, I don't need coverage</p>
  </div>
  <script>
  function redirectPay() {
    window.confirm("No coverage selected. You will be redirected to My Phone Warehouse...");
  }
  </script>
<% end %>
